# Override to expose MySQL on the host. Use only if you stop your host MySQL service.
# To enable: stop local mysqld, then run `docker compose up --build -d` (compose merges this file automatically).

services:
  mysql:
    ports:
      - "3306:3306"
