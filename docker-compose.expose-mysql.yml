# Override to expose MySQL on the host. Use only if you stop your host MySQL service.
# To enable: stop local mysqld, then run `docker compose -f docker-compose.yml -f docker-compose.expose-mysql.yml up --build -d`

services:
  mysql:
    ports:
      - "3306:3306"
